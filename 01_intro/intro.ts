let greet: string = "Hello buddy!!";

console.log(greet.toUpperCase());
console.log("Vraj");

// Assuming 'user' is defined somewhere else in your code
// console.log(user.name);

let num: number = 10;

num = 20; // Assigning a number instead of a string
console.log(num);

export {};
